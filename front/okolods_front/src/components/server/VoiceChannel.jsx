export function VoiceChannel(props) {
    return (
        <div className="server_channel_container">
            <p className="server_channel_name_text">{props.name}</p>
            <p className="server_channel_member_text">test</p>
        </div>
    );
}